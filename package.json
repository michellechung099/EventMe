{
  "dependencies": {
    "react-icons": "^4.8.0"
  },
  "name": "eventme",
  "description": "event creation and search app based on location.",
  "version": "1.0.0",
  "directories": {
    "lib": "lib"
  },
  "devDependencies": {},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm install --prefix frontend && npm run build --prefix frontend"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "engines": {
    "node": "16.19.0"
  }
}
